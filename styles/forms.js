import { StyleSheet } from "react-native";

const forms = StyleSheet.create({
  title: {
    fontSize: 14,
    fontWeight: "bold",
    textAlign: "center"
  },
  input: {
    margin: 5
  }
})

export default forms;